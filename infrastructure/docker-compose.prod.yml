services:
  caddy:
    image: caddy:2.8-alpine
    restart: unless-stopped
    env_file:
      - ${PRIVATE_ENV_FILE:-../private/.env}
    depends_on:
      frontend:
        condition: service_started
      backend:
        condition: service_started
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

  frontend:
    image: ghcr.io/cleancentive/cleancentive-frontend:13654cbe7b7ddf59836a3e49a9f627ec0e00f218
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "test -f /usr/share/nginx/html/index.html"]
      interval: 30s
      timeout: 5s
      retries: 3

  backend:
    image: ghcr.io/cleancentive/cleancentive-backend:13654cbe7b7ddf59836a3e49a9f627ec0e00f218
    restart: unless-stopped
    env_file:
      - ${PRIVATE_ENV_FILE:-../private/.env}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "bun", "-e", "await fetch('http://127.0.0.1:3000/api/v1/docs').then((response) => { if (!response.ok) process.exit(1); });"]
      interval: 30s
      timeout: 5s
      retries: 5

  worker:
    image: ghcr.io/cleancentive/cleancentive-worker:13654cbe7b7ddf59836a3e49a9f627ec0e00f218
    restart: unless-stopped
    env_file:
      - ${PRIVATE_ENV_FILE:-../private/.env}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  postgres:
    image: postgis/postgis:15-3.4
    restart: unless-stopped
    env_file:
      - ${PRIVATE_ENV_FILE:-../private/.env}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-cleancentive} -d ${DB_DATABASE:-cleancentive}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

volumes:
  caddy_data:
  caddy_config:
  postgres_data:
  redis_data:
