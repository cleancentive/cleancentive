---
feedback_id: 019dee68-844b-713c-8670-f6206c9cc025
title: Cleanup creation — text editor behaves oddly (Adelboden)
status: open
category: bug
severity: high
prod_status: new
created: 2026-05-06
last_synced: 2026-05-06
last_updated: 2026-05-06
related: []
---

## Original Feedback
> Erstellen des Cleanups, irgendwas komisch im Text editor. Siehe Adelboden

(Translation: When creating the cleanup, something is weird in the text editor. See Adelboden.)

## Error Context
None

## Conversation Thread
No responses yet

## Analysis
The cleanup description field is a plain HTML `<textarea>` — there is no rich-text editor wired up (no Tiptap, Lexical, ProseMirror; nothing in package.json). Both [frontend/src/components/CleanupList.tsx:160](../../../frontend/src/components/CleanupList.tsx) (creation) and [frontend/src/components/CleanupDetail.tsx:422](../../../frontend/src/components/CleanupDetail.tsx) (edit) use the same plain textarea.

"Weirdness" without specifics could mean: textarea height jumping, text wrapping, paste-from-mobile inserting odd characters, RTL/LTR cursor jumps, soft-keyboard scroll-jank on iOS, or the cleanup description stored newlines being rendered without `white-space: pre-wrap` on view. The reference to "Adelboden" suggests a specific cleanup whose stored description renders incorrectly.

Action needs: load the Adelboden cleanup, inspect what is stored vs displayed, then fix.

## Implementation Plan
1. **Reproduce on prod** — fetch the Adelboden cleanup detail (via API or UI), capture the raw description value and how it renders on the create/edit form vs the public detail page.
2. **Likely first fix — preserve newlines on display.** If description renders as one long line: add `white-space: pre-wrap` or convert `\n` to `<br>` in the public render path (search for where `cleanup.description` is rendered read-only).
3. **Likely second fix — autosize textarea.** If the issue is the textarea jumping height or being too small, add `rows`/`maxRows` autosize (no library needed — small useEffect on `scrollHeight`).
4. **Ask the reporter for a screenshot** if the above diagnostics don't surface the issue. Use the feedback response API.

## Files to Modify
- frontend/src/components/CleanupList.tsx (creation textarea)
- frontend/src/components/CleanupDetail.tsx (edit textarea + read-only render)
